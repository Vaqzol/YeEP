# 🚌 YeEP - ระบบจองที่นั่งรถบัส มทส.

ระบบจองที่นั่งรถบัสภายในมหาวิทยาลัยเทคโนโลยีสุรนารี พัฒนาด้วย Flutter + Spring Boot

## 📋 สารบัญ

- [ภาพรวมโปรเจค](#ภาพรวมโปรเจค)
- [โครงสร้างโปรเจค](#โครงสร้างโปรเจค)
- [วิธีติดตั้งและรัน](#วิธีติดตั้งและรัน)
- [หลักการ OOP ที่ใช้](#หลักการ-oop-ที่ใช้)
- [ผู้พัฒนา](#ผู้พัฒนา)

---

## 📱 ภาพรวมโปรเจค

**YeEP** เป็นแอปพลิเคชันสำหรับจองที่นั่งรถบัสภายในมหาวิทยาลัยเทคโนโลยีสุรนารี ช่วยให้นักศึกษาและบุคลากรสามารถ:
- ค้นหาเส้นทางรถบัส
- ดูเที่ยวรถและที่นั่งว่าง
- จองที่นั่งล่วงหน้า
- ดูประวัติการจอง
- ติดตามตำแหน่งรถแบบ Real-time

---

## 📁 โครงสร้างโปรเจค

```
proj6393/
├── yeep_app/           # Flutter Mobile App
├── yeep_backend/       # Spring Boot Backend (Java)
├── gps-simple/         # GPS Server (Java)
└── docs/               # เอกสารและรายงาน
```

### Technology Stack

| ส่วน | เทคโนโลยี |
|------|-----------|
| **Mobile App** | Flutter (Dart) |
| **Backend** | Spring Boot (Java) |
| **Database** | PostgreSQL / H2 |
| **GPS Server** | Java HttpServer |

---

## 🚀 วิธีติดตั้งและรัน

### ข้อกำหนดเบื้องต้น

- Java 17+
- Flutter 3.x
- Maven 3.x

### 1️⃣ รัน Backend

```bash
cd yeep_backend
./mvnw spring-boot:run
```
Backend จะรันที่ `http://localhost:8081`

### 2️⃣ รัน GPS Server

```bash
cd gps-simple
javac -encoding UTF-8 -d out src/Main.java src/server/*.java src/service/*.java src/model/*.java src/adapter/*.java
java -cp out Main
```
GPS Server จะรันที่ `http://localhost:8090`

### 3️⃣ รัน Flutter App

```bash
cd yeep_app
flutter pub get
flutter run
```

---

## 🎯 หลักการ OOP ที่ใช้

### สรุปจำนวน Class และ Method

| ประเภท | จำนวน Class |
|--------|-------------|
| Entity | 5 |
| Service | 5 |
| Controller | 3 |
| Repository | 4 |
| DTO/Util | 3 |
| **รวม** | **20 classes** |

| Class | จำนวน Method |
|-------|-------------|
| BookingService | 21 |
| UserService | 18 |
| BusRouteService | 6 |
| BusTripService | 5 |
| FileService | 15 |
| BookingController | 16 |
| UserController | 8 |
| FileController | 4 |
| **รวม** | **93 methods** |

### หลักการ OOP ที่ใช้

| หลักการ | ไฟล์ | คำอธิบาย |
|---------|------|----------|
| **Inheritance** | `BaseEntity.java` | Abstract class ที่ Entity อื่นสืบทอด |
| **Polymorphism** | `JpaRepository` | Interface ที่ Repository ทั้ง 4 ตัว implement |
| **Aggregation** | Entity relationships | ความสัมพันธ์ระหว่าง BusRoute → BusTrip → Booking |
| **File Input** | `FileService.java` | อ่าน/เขียนไฟล์, อัพโหลดรูปโปรไฟล์ |
| **Data Sorting** | `BookingService.java` | **Selection Sort** O(n²) |
| **Data Searching** | `BusRouteService.java` | **Sequential Search** O(n) |

> 📄 ดูรายละเอียดเพิ่มเติมที่ `docs/oop_concepts_java.md`

---

## 📊 API Endpoints

### User API
| Method | Endpoint | คำอธิบาย |
|--------|----------|----------|
| POST | `/api/login` | เข้าสู่ระบบ |
| POST | `/api/register` | ลงทะเบียน |
| GET | `/api/user/{username}` | ดึงข้อมูลผู้ใช้ |

### Booking API
| Method | Endpoint | คำอธิบาย |
|--------|----------|----------|
| GET | `/api/booking/routes` | ดูเส้นทางทั้งหมด |
| GET | `/api/booking/search` | ค้นหาเส้นทาง |
| GET | `/api/booking/trips` | ดูเที่ยวรถ |
| POST | `/api/booking/book` | จองที่นั่ง |
| DELETE | `/api/booking/cancel/{id}` | ยกเลิกการจอง |

### File API
| Method | Endpoint | คำอธิบาย |
|--------|----------|----------|
| POST | `/api/files/profile/{username}` | อัพโหลดรูปโปรไฟล์ |
| GET | `/api/files/profile/{username}` | ดึงรูปโปรไฟล์ |

---

## 👥 ผู้พัฒนา

**กลุ่ม xxx - วิชา 1101093 Object-Oriented Programming**

มหาวิทยาลัยเทคโนโลยีสุรนารี

---

## 📄 License

This project was developed for educational purposes as part of the OOP course at SUT.
